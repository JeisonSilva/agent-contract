export default class Executor {
    private _executorRoot: any;
    private _toolboxRoot: any;

    constructor(executorRoot: any, toolboxRoot: any) {
        this._executorRoot = executorRoot;
        this._toolboxRoot = toolboxRoot;
    }

    
}